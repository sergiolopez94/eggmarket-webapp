{
  "endpoint": "get-location",
  "description": "Get a specific location",
  "method": "GET",
  "path": "/{{company_id}}/locations/{{locationId}}",
  "path_params": [
    { "name": "company_id", "type": "uuid", "required": true },
    { "name": "locationId", "type": "uuid", "required": true }
  ],
  "query_params_supported": ["include"]
}