{
  "endpoint": "list-locations",
  "description": "List all warehouse and storage locations",
  "method": "GET",
  "path": "/{{company_id}}/locations",
  "path_params": [
    { "name": "company_id", "type": "uuid", "required": true }
  ],
  "query_params_supported": ["include", "filter", "sort", "page", "limit"]
}